BACKEND_PORT=3000
FRONTEND_PORT=4321
DB_PORT=5432

# Optional override for dashboard container proxy target in Docker
API_PROXY_TARGET=http://backend:8080

# Optional local CORS override for backend (non-Docker local runs)
FRONTEND_ORIGIN=http://localhost:4321
